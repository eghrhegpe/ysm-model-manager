# v1-9-0 - Work Plan

## TL;DR (For humans)
<!-- Fill this LAST, after the detailed plan below is written, so it summarizes the REAL plan. -->
<!-- Plain English for a non-engineer: NO file paths, NO todo numbers, NO wave/agent/tool names. -->

**What you'll get:** A new compact list view for your file tree (toggle via toolbar button, remembers your choice), smoother scrolling without flicker, and safety-net tests for the auto-updater's version comparison logic.

**Why this approach:** The list view is the most user-visible improvement from the backlog. The flicker fix is a cheap one-line DOM reuse that piggybacks on the same render changes. Updater tests protect the release pipeline with minimal effort. We skipped frontend test expansion because it delivers zero user value.

**What it will NOT do:** Add tests for unrelated frontend modules, cache 2D skeleton previews, or change any page other than the repository tree.

**Effort:** Short (~2-3 days)
**Risk:** Low — all changes are additive or localized to a single component; updater tests are pure logic with no side effects.
**Decisions to sanity-check:**
- List row height is 24px (compact) — okay if names feel cramped on small screens?
- All three items ship together as v1.9.0 — no beta preview.

Your next move: Run `$start-work` (or equivalent) to begin execution. Full execution detail follows below.

---

> TL;DR (machine): Short effort, low risk. Deliverables: list/grid toggle with localStorage, virtual-scroll flicker fix via DOM reuse, updater Go tests for normalize/isNewer/splitVer/fetchExpectedHash. Skips frontend tests and model2d cache.

## Scope
### Must have
1. List/grid view toggle for the repository file tree (toolbar button, compact list template, localStorage persistence).
2. Virtual scroll flicker fix — reuse the `.vs-wrap` DOM element instead of recreating it on every scroll/frame.
3. Go unit tests for `updater` package: `normalize`, `isNewer`, `splitVer`, `fetchExpectedHash`.
4. Build verification (frontend + Go) and update `docs/TASK_PLAN.md` to mark #6 and #7 complete.

### Must NOT have (guardrails, anti-slop, scope boundaries)
- Do NOT add frontend unit tests for unrelated modules (task #5).
- Do NOT implement model2d preview cache.
- Do NOT introduce new npm/Go dependencies.
- Do NOT refactor virtual-scroll architecture beyond the minimal DOM-reuse fix.
- Do NOT change other pages (community, settings, 3D preview, etc.).
- Do NOT delete or disable the existing grid view.

## Verification strategy
> Zero human intervention - all verification is agent-executed.
- Test decision: tests-after + Vitest (frontend build only, no new tests) / Go `testing` + `httptest`
- Evidence: `.omo/evidence/task-<N>-v1-9-0.<ext>` (screenshots, build logs, test output)

## Execution strategy
### Parallel execution waves
- **Wave 1**: Todo 1 (list/grid toggle) — touches frontend templates, render, virtual-scroll.
- **Wave 2**: Todo 2 (flicker fix) — depends on Wave 1 because it modifies `renderSlice()` which Wave 1 also touches.
- **Wave 3**: Todo 3 (updater test) — independent Go work, can run in parallel with Wave 1 or 2.
- **Wave 4**: Todo 4 (build verification + docs) — depends on Waves 1-3.

### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1 List/grid toggle | — | 2, 4 | 3 |
| 2 Flicker fix | 1 | 4 | — |
| 3 Updater test | — | 4 | 1 |
| 4 Build + docs | 1, 2, 3 | — | — |

## Todos
> Implementation + Test = ONE todo. Never separate.
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->
- [ ] 1. List/grid view toggle for repository tree
  What to do / Must NOT do:
  - Create `frontend/js/components/app-tree/row-tpl-list.js` with `listFileRowHTML()` and `listFolderRowHTML()`.
    - List row: 24px height, shows icon + display name + size (no hover actions, no date, no tag dot).
    - Folder row: 24px height, shows arrow + folder icon + name.
    - Keep the same `data-path`, `data-fullpath`, `data-dir`, `class="ck/on/ban"` attributes so selection/ban logic is unchanged.
  - Modify `frontend/js/components/app-tree/virtual-scroll.js`:
    - Export `ROW_H_GRID = 28` and `ROW_H_LIST = 24`.
    - Change `calcVisibleRange(container, totalRows, rowH)` to accept a dynamic row height parameter.
  - Modify `frontend/js/components/app-tree/tpl.js`:
    - Add `<button class="btn-base sm" id="btn-view-mode" title="切换视图">☰</button>` to `headerHTML()` (before the sort select).
    - Default icon: ☰ (list icon). When clicked, toggle to 🗂 (grid icon) and back.
  - Modify `frontend/js/components/app-tree/render.js`:
    - `flattenVisible()` accepts a `mode` parameter (`'grid' | 'list'`); call `fileRowHTML`/`folderRowHTML` for grid, new list templates for list.
    - `renderSlice()` accepts `rowH` parameter and passes it to `calcVisibleRange()`.
    - `renderTree()` reads `localStorage.getItem('ysm-render-mode') || 'grid'` on every call.
    - Install a one-time click listener on `#btn-view-mode` (guard with `document.getElementById('btn-view-mode')._vsToggleInstalled`) that toggles mode, writes `localStorage`, and re-renders.
  - Add CSS in `frontend/css/app-tree.css` (or equivalent tree stylesheet):
    - `.fl-list { height:24px; display:flex; align-items:center; gap:8px; padding:0 8px; }`
    - `.fh-list { height:24px; display:flex; align-items:center; gap:8px; padding:0 8px; }`
    - Hide `.hover-actions`, `.dt`, `.tag-dot` when inside `.fl-list`.
  Must NOT do: delete grid templates; change selection/ban logic; change other pages.
  Parallelization: Wave 1 | Blocked by: — | Blocks: 2, 4
  References (executor has NO interview context - be exhaustive):
    - `frontend/js/components/app-tree/row-tpl.js` — existing grid templates (lines 1-82)
    - `frontend/js/components/app-tree/virtual-scroll.js` — ROW_H and calcVisibleRange (lines 1-36)
    - `frontend/js/components/app-tree/tpl.js` — headerHTML toolbar (lines 3-24)
    - `frontend/js/components/app-tree/render.js` — renderSlice, flattenVisible, renderTree (lines 88-250)
    - CSS file: find with `glob("frontend/css/*tree*")` or `glob("frontend/css/app-tree*")`
  Acceptance criteria (agent-executable):
    - `cd frontend && npx vite build 2>&1 | Select-String error` returns nothing.
    - Toggle button exists in DOM: `document.getElementById('btn-view-mode')` is non-null after tree render.
    - localStorage round-trip: set `'list'` → reload → mode is `'list'`; set `'grid'` → reload → mode is `'grid'`.
    - Both modes render without JS errors when tree has files + folders.
  QA scenarios (name the exact tool + invocation): happy + failure, Evidence `.omo/evidence/task-1-v1-9-0.md`
    - Happy: Open repo page, click toggle → view switches to list, rows are 24px, click again → back to grid.
    - Failure: Empty tree → toggle should still render empty state correctly.
  Commit: Y | feat(app-tree): add list/grid view toggle with localStorage persistence

- [ ] 2. Fix virtual scroll flicker by reusing `.vs-wrap` DOM
  What to do / Must NOT do:
  - Modify `frontend/js/components/app-tree/render.js` `renderSlice()` (lines 169-192):
    - Instead of `container.innerHTML = '<div class="vs-wrap">...</div>'`, first query `container.querySelector('.vs-wrap')`.
    - If it exists, update `style.paddingTop`, `style.paddingBottom`, and `innerHTML` directly.
    - If not, create the div, set className, append to container, then set content.
    - Pass `rowH` parameter through to calcVisibleRange (already changed in Todo 1).
  Must NOT do: introduce a virtual DOM library; change scroll listener logic; change row HTML generation.
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: 4
  References (executor has NO interview context - be exhaustive):
    - `frontend/js/components/app-tree/render.js` — renderSlice lines 169-192
    - `frontend/js/components/app-tree/virtual-scroll.js` — calcVisibleRange lines 12-18
  Acceptance criteria (agent-executable):
    - `cd frontend && npx vite build 2>&1 | Select-String error` returns nothing.
    - Scroll the tree rapidly; no visible white flashes or full repaints.
    - DevTools Performance tab: no large "Recalculate Style" spikes on scroll (qualitative).
  QA scenarios (name the exact tool + invocation): happy + failure, Evidence `.omo/evidence/task-2-v1-9-0.md`
    - Happy: Scroll 1000-item tree — smooth, no flicker.
    - Failure: Resize window to 0 height then restore — should recover without crash.
  Commit: Y | perf(render): reuse .vs-wrap DOM to eliminate scroll flicker

- [ ] 3. Add Go unit tests for updater package
  What to do / Must NOT do:
  - Create `go/updater/update_test.go`.
  - `TestNormalize` — table-driven: inputs `"v1.2.3"`, `"  v1.2.3  "`, `"1.2.3"`, `"v1.2.3-beta"` → expect `"1.2.3"`, `"1.2.3"`, `"1.2.3"`, `"1.2.3-beta"`.
  - `TestIsNewer` — table-driven: pairs `("1.2.3", "1.2.2", true)`, `("1.2.3", "1.2.3", false)`, `("1.3.0", "1.2.9", true)`, `("1.2", "1.2.3", false)`, `("1.2.3", "1.2", true)`.
  - `TestSplitVer` — table-driven: `"1.2.3-beta"` → `[1,2,3]`, `"1.2.3+build"` → `[1,2,3]`, `"1.a.3"` → `[1,0,3]`, `"2"` → `[2]`.
  - `TestFetchExpectedHash` — start an `httptest` server returning a SHA256SUMS body, call `fetchExpectedHash(server.URL+"/sums", "YSM-Model-Manager_windows_amd64.zip")`, assert returned hash equals expected.
  Must NOT do: test `Check` (real GitHub API), `Download` (large file/network), `InstallUpdate` (system side-effects).
  Parallelization: Wave 3 | Blocked by: — | Blocks: 4
  References (executor has NO interview context - be exhaustive):
    - `go/updater/update.go` — normalize (327-329), isNewer (331-340), splitVer (342-356), fetchExpectedHash (368-404)
    - `go/dedup/dedup_test.go` — reference test style (lines 1-148)
    - Go `net/http/httptest` package for mock server.
  Acceptance criteria (agent-executable):
    - `go test ./go/updater/... -v` passes with 4+ test cases, 0 failures.
    - `go build ./go/...` passes with 0 errors.
  QA scenarios (name the exact tool + invocation): happy + failure, Evidence `.omo/evidence/task-3-v1-9-0.md`
    - Happy: `go test ./go/updater/... -v` → all PASS.
    - Failure: Introduce a deliberate bug in `isNewer` (e.g., flip `>` to `<`) → tests FAIL, then revert.
  Commit: Y | test(updater): add unit tests for normalize, isNewer, splitVer, fetchExpectedHash

- [ ] 4. Build verification and TASK_PLAN.md update
  What to do / Must NOT do:
  - Run `cd frontend && npx vite build 2>&1 | Select-String error` — must return nothing.
  - Run `go build ./go/... 2>&1 | Select-String error` — must return nothing.
  - Run `go test ./go/updater/...` — must pass.
  - Update `docs/TASK_PLAN.md`:
    - Task #6 (Go 测试扩展): status → `✅ 完成 (v1.9.0)`
    - Task #7 (列表/网格视图切换): status → `✅ 完成 (v1.9.0)`
  Must NOT do: modify unrelated tasks; commit without explicit user request.
  Parallelization: Wave 4 | Blocked by: 1, 2, 3 | Blocks: —
  References (executor has NO interview context - be exhaustive):
    - `docs/TASK_PLAN.md` — task table lines 10-21
  Acceptance criteria (agent-executable):
    - All three build/test commands exit successfully.
    - TASK_PLAN.md shows #6 and #7 as complete with v1.9.0 annotation.
  QA scenarios (name the exact tool + invocation): happy + failure, Evidence `.omo/evidence/task-4-v1-9-0.md`
    - Happy: Full build + test suite green.
    - Failure: Deliberately break a Go test → `go test` fails → fix → green.
  Commit: Y | docs(task-plan): mark #6 and #7 complete for v1.9.0

## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE. Surface results and wait for the user's explicit okay before declaring complete.
- [ ] F1. Plan compliance audit — verify every changed file matches the todo references; no files were modified outside the scope.
- [ ] F2. Code quality review — run `lsp_diagnostics` on all changed files; zero errors required. Check for `as any`, empty catch blocks, dead code.
- [ ] F3. Real manual QA — open the app, load a repo with 50+ files, toggle list/grid 3 times, scroll rapidly in both modes, verify no flicker and selection persists.
- [ ] F4. Scope fidelity — confirm no changes to community page, settings, 3D preview, or unrelated modules. Confirm no new dependencies in `package.json` or `go.mod`.

## Commit strategy
- One commit per todo (atomic, descriptive).
- Commit messages follow conventional commits (`feat()`, `perf()`, `test()`, `docs()`).
- Do NOT amend commits; if a fix is needed, add a new commit.
- Push only if explicitly requested by user.

## Success criteria
- User can toggle between list (24px compact) and grid (28px detailed) views via toolbar button.
- View mode persists across app restarts via localStorage.
- Virtual scroll in both modes is smooth with no flicker on rapid scroll.
- `go test ./go/updater/...` passes with 100% coverage of targeted functions.
- Frontend and Go builds are clean (zero errors).
