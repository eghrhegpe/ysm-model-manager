# frontend/src 架构锐评处置状态（2026-09-03）

> 用户 @long-text「frontend/src 架构锐评」(按等级) 的逐项处置跟踪。
> 锐评原文结论：桌面主链路职责红线守得住，雷集中在 browser parity 层（S2）与 visibleWhen 双轨（S1）。
> 处置铁律：先查证后动手、行为有测试锁定、知识卡同步、commit 白名单纯净。

## 严重（5 项）

| 项 | 条目 | 状态 | 处置 |
|---|---|---|---|
| S1 | dock 可见性走专有布尔（visibleWhen 双轨） | ✅ 闭环 | `1635fcd4` 删三布尔、状态层扩 ui.mode/env.skyGroundCap、dock 与内容级同一 visibleWhen 求值器 |
| S2 | browser-adapter 整层 TS 复刻 Go 扫描/判定 | ⏳ 排期(ADR 已立) | `ADR-174` 已立：类型归属单一源**查证已成立**（Go registry=内嵌 resource_types.json / TS 直读，双端零手写）；真双实现区=格式平移层，以共享 fixtures 对账硬锁（D2）+ 差异分类声明制（D3）+ contract-b1 唯一哨兵（D4）。首轮四函数漂移审计见 ADR 附录 A：SearchModels kw 降级语义/排序未锁为契约、DetectContainerType 上限联动、ExtractYsmSummary Size 口径、voxel TS↔Go 缺 fixture——收口定义=D5 全落地；`0406fbf4` 已锁 B1c（kw 快路径降级语义 = web 契约，Go 差异显式声明）+ web-fs.ts D3/附录 A 三处注释（降级不排除不排序 / 容器探测上限联动）；剩 fixtures 基建（双端语料 + 镜像对账测试，须 go/ 并行静默后动） |
| S3 | 树搜索/排序/筛选在前端本地做 | ✅ 闭环 | 查证：SearchModels 是磁盘级数值范围搜索（adv-filter 消费），树内 search/排序作用在 Go 已交付内存 entries 属 UI 交互——下沉 RPC 不成立；AGENTS.md:22 红线加豁免注脚（输入端归 Go / 展示端豁免）+ render.ts 代码锚点 |
| S4 | preview-3d 反向 import views（层级倒置） | ✅ 闭环 | `edceb408` 6 纯类型迁 content-bridges.ts，preview-3d → views import 清零 |
| S5 | dock 渲染硬编码组专属捷径 | ✅ 闭环 | `f70bbf3f` PreviewMenuGroupDef.directToPanel 静态声明，renderPreviewDock 删 if (g.id==="model")，motion 动态特例显式标注 |

## 一般（6 项）

| 项 | 条目 | 状态 | 处置 |
|---|---|---|---|
| G1 | show 侧手写扩展名分支 | ✅ 闭环 | `870a0c13` EntityPlayer:vrm 复合 key 条目 + statsCardHTML extOf + litematic isContainerExt（顺带修 .7z 蓝图缺陷） |
| G2 | Go 扫描结果二次前端过滤 | ✅ 闭环 | `e264e7ac` previewCandidateExtsOf JSON 派生；查证修正：Go 全 ext 白名单=类型归属语义（下沉破坏），收窄是预览候选语义，正确修法是 JSON 派生非改 Go |
| G3 | 3D 菜单手写 fill* 飞地 | ✅ 闭环 | `bf1459a4` fill* 三函数删除（生产零调用）+ `72573102` renderCustom 构造点审计门（3 豁免白名单锁死）；P1 收官时移交的死代码复查清单亦闭环（2026-09-04）：ui-preset.ts 整模块（buildPresetChipGroup/addClearRow，生产零引用仅测试直测）、ui-rows addWatchDirRow（3 处 cssText）、skeleton-utils.ts 整文件（sec/iRow/buildDepthMap，生产仅 skeleton-render.test 直测）全部删除 + 对应测试块移除，P1 cssText 账面归零 |
| G4 | 渲染链路多通道衰退 | ✅ 闭环 | `383d7c1f` G3 后 fillers 仅 roles 独苗，四路互斥分派注释收敛 + health.test fillers roles-only 白名单守卫 |
| G5 | community 回收站过滤前端复刻 Go | ✅ 闭环 | `39114d99` utils/recycle-path.ts hasRecycleSegment 单一实现（双复刻删并）；口径修正：真对齐对象是 sync.hasRecycleSegment 非 IsRecycleDir |
| G6 | 3D overlay light DOM（全站 Shadow DOM 不一致） | ✅ 闭环（ADR-175，M1 落地 + D2 修订） | 立项（ADR-175，2026-09-04）→ **M1 落地（`d8dba486`）**：`#ysm-overlay-3d` 改 shadow host（id/class/aria 挂 host，app-tree:296 守卫零改动），tip/body/viewContainer/菜单链内容全量迁入 shadowRoot；共享样式模块 adoptedStyleSheets 安装 + 新增 `overlay-style-bridge.ts`（8 个 ensure*Styles + ensureFabStyles 注入目标经桥迁移，目标切换自动复位旗标重注入；无 overlay 时 head 兜底，menu 族单测路径不变）；attachShadow 缺失环境（behavior.test fake doc）降级 light DOM；PreviewBuildCtx/SwitchContext.overlay 放宽 `HTMLElement \| ShadowRoot` 并传 root；litematic-3d.test lastOverlay() 返回 shadowRoot 作用域。**D2 修订（2026-09-04）**：M1 后菜单 DOM 已整体居于 overlay shadow 树内、封装成立，createSlideMenu 与同层 🥉 元素工厂形态一致——再套第二层 shadow 成本 ~90 处测试双级穿透、无链外复用收益，**M2 取消**；将来链外独立消费再按 opts.shadow 开关立项。**M3 查证自然满足**：单元侧随 M1 落地（scope() 优先 shadowRoot）；e2e/preview.spec.ts 无 `#ysm-overlay-3d` 触达用例（全目录 0 处引用），无穿透改造对象。验证：全量套件 328 文件 5191 测试全绿 + vite build 绿。状态卡纠错历史：2026-09-03 曾误计闭环，此次为真实闭环 |

## 风格（4 项）

| 项 | 条目 | 状态 | 处置 |
|---|---|---|---|
| P1 | 137 处 style.cssText 内联样式 | ✅ 活代码清零（12 批次） | 勘察报告 `frontend-src-critique-g6p1-survey.md`：实测 139 处中**静态 134（96.4%）、动态仅 5**（cap-controls×3/vrm-bone-ui/skeleton-fill-panel，滑块类插值豁免）——先前"大量动态不可静态化"推断被数据推翻；**范式有链内实证**（render.ts:20 已实践 cssText→类 + `<style>` 注入）。**批次1 render.ts 15/16 闭环**（`8d6a2a8b`，13 新类 rm-*，双类锚定防注入顺序依赖；body display 折叠态动态豁免）。**批次2 cap-controls.ts 37/37 全清**（`7c2c0604`，27 类进幂等 ensureCapStyles 注入、renderCapControls 入口调用覆盖 env 直调/render 委托全路径；header/arrow 镜像复用 cap-section-header/arrow **双源标注待共享模块化**；band/thumb-img 动态插值拆 height/width 内联属性归零 cssText；body display:block 冗余删）。**批次3 roles.ts 18/18 全清**（`e79eee75`，2026-09-04）：幂等 ensureRolesStyles 注入（modelDetailView/motionDetailView/fillRoles 三入口调用）；高亮单一源 `.fr-row-active` 共享角色 active 与组件当前行（派生 --accent）；radio 变体色折进 `.fr-row-active` 后代选择器、isCur/isActive 条件全类 token 化；`frRoleRowStyle`→`frRoleRowClass` 类 token 缝，测试改断「类 token+注入样式表原文」两级（happy-dom 不认 color-mix 的旧因由样式表原文断言承接）。**批次4 env.ts 14/14 全清**（`ef131ec5`，2026-09-04）：幂等 ensureEnvStyles 注入（renderEnvLevel 唯一入口调用覆盖平铺/两级菜单全路径）；双类锚定 `.slide-item.ev-row`/`.ev-group-entry`/`.slide-label.ev-label-grow` 压 ui 单类；lb×3/chevron×2 收敛；ev-range/ev-label-grow/ev-empty-note 三处与 cap/roles 同值**双源镜像标注待共享模块化**；hasSub cursor 条件按 render.ts body.display 同款**动态豁免留内联属性**（env.test.ts:122 断言 style.cursor，抽类无法被该 DOM 级断言读回）。剩 54 处（switch 7/skeleton-fill-panel 6/vrm-bone-ui 6/core 5/mount-preview-core 5/camera-controls 4/zoom 3…）。**批次5 switch.ts 7/7 全清**（`8f7e9838`，2026-09-04）：幂等 ensureSwitchStyles 注入（fillSwitch 唯一入口调用）；tab 激活高亮单一源化——`.sw-tab-active` 规则背景由 `switchTabHighlightBg(true)` 模板插值派生（刀② 收编防二次漂移），运行时 highlightTab 改 classList.toggle、删无外部引用的 switchTabCssText、新增内部 switchTabClass 类 token 缝；候选行 isCur 高亮收 `.sw-row-cur`；core.test 476-478 改断类归属两级。**批次2 回归修复**（`9028275f`）：adapters/preview-menu.test 6 项内联样式探测回归（批次2 menu 全族未覆盖 adapters 目录的验证缺口）——section body 补初始内联 display:block 动态豁免、divider/button/preset-thumb/timeline band 探测改类级；此后以**全量套件 329 文件/5214 测试**为每批认证门槛。剩 47 处（skeleton-fill-panel 6/vrm-bone-ui 6/core 5/mount-preview-core 5/camera-controls 4/zoom 3/path-cards 3/ui-rows 3…）。**批次6 core.ts 5/5 全清**（`7c621648`，2026-09-04）：幂等 ensureCoreStyles 注入（buildPreviewMenuShell+makePreviewMenuRow 双入口调用）；`.ysm-preview-menu.cm-popup` 双类锚定承载静态布局，显隐 display 留内联动态豁免（core/roles.test 断言 style.display）；cm-row/cm-row-icon/cm-error-note 镜像 switch/roles（双源标注）；items.test errRow 探测改 `.cm-error-note` 类+样式表原文两级。**menu 区 cssText 归零**（剩 render.ts body.display 1 处动态豁免，其余 render/roles/env/cap/switch/core 全 0）。剩 42 处（skeleton-fill-panel 6/vrm-bone-ui 6/mount-preview-core 5/camera-controls 4/zoom 3/path-cards 3/ui-rows 3…）。**批次7 skeleton-fill-panel.ts 6/6 全清**（`40446216`，2026-09-04）：幂等 ensureSfpStyles 注入（fill3DPanel legacy 命令式入口——ysm 已 schemaId 化，仅 skeleton.test 仍执行）；sec 边框变体拆 `.stat-section.sfp-sec`(+`.sfp-sec-bordered`)，stat-section/stat-row 语义锚点类保留；iRow/texRow/cap/left/right 收 sfp-* 类；vSpan/cap.innerHTML 的 style.color 属性级赋值非 cssText 范畴未动。剩 36 处：vrm-bone-ui 6/mount-preview-core 5/camera-controls 4/zoom 3/path-cards 3/ui-rows 3…（含 render.ts body.display 动态豁免 1）。注：app-modules.boot 间歇 flake 二次复现（单跑 14/14 绿，启动装配域非改动域）。**批次8 vrm-bone-ui.ts 7/7 全清**（`08431d11`，2026-09-04，adapters 域首个样式注入）：幂等 ensureVbuStyles 注入（renderer 体首行，重入安全）；panel `cssText +=` **重入累积隐患改属性级赋值幂等**（容器由框架传入非本模块私有，增量声明豁免注释）；row depth 动态 padding-left 拆属性赋值（vrm-bone-ui.test:52-54 断言 style.paddingLeft），静态布局入 `.slide-item.vbu-bone-row`；empty/label/detail 双类锚定压 ui 单类（slide-sublabel 亮色无 padding/slide-label 无 overflow）；bone-detail-inline 语义锚点类保留（测试 querySelector .bone-detail-inline）。**批次9 camera-controls 4/4 + mount-preview-core 5/5 全清**（`48395bbe`，2026-09-04，adapters 域 cssText 收尾归零）：cam- 3 类（rot/spdLabel 同值收敛）+ ensureCamStyles 注入（buildCameraControls 入口）；mpc- 5 类（overlay 模态层/body/view/loading/tip 全静态容器）+ ensureMpcStyles 挂 installUiComponentsStyles 旁（mount3D 头部，幂等；behavior.test mock document 有 head 兼容）；`.preview-view-container.mpc-view` 双类——语义锚点类保留 + 布局类防将来锚点规则覆盖，behavior.test 451/465 className 严格相等改 `?.includes`（双类适配，createdElements className 可选链）。剩 21 处：zoom 3/path-cards 3/ui-rows 3/skeleton-utils 2/toolbar-search 1/skeleton 1/detail 1/keymap 1/ui-preset 1（含 render.ts body.display 动态豁免 1）。**批次10 zoom.ts 3/3 + skeleton.ts 1/1 + detail.ts 1/1 全清**（`8fe403a0`，2026-09-04）：zoom overlay 挂 body light DOM——ensureZoomStyles head 注入 + 3 类（zoom-overlay/canvas/hint）；skeleton/detail 元素在 **shadow root 内（head 注入失效）**——规则追加进手编 previewCSS（css.ts，非自动生成可编），类 sk-loading-box（loadModel2D 运行时 opacity=1 属性赋值覆盖类注释）/dp-stats。**skeleton-utils.ts 死导出标注不抽类**：sec/iRow/buildDepthMap 生产零消费（仅 skeleton-render.test.ts:22 直测"审核盲区补建"），疑似历史遗留死代码留 G3 复查——不为死代码抽类。剩 16 处：path-cards 3/ui-rows 3/skeleton-utils 2（死代码留档）/toolbar-search 1/keymap 1/ui-preset 1/litematic-adapter 1（含 render.ts 动态豁免 1；活代码实为 13）。**批次11 path-cards 3/3 + toolbar-search 1/1 + litematic-adapter 1/1 全清**（`ef8971d0`，2026-09-04）：三处元素查证均最终挂 **light DOM**（path-cards 模态经 `getRootNode()===document` 判断提升 host 父级/body、ts-badge 挂 body、mdli 警告插 3D overlay）→ head 注入适用：ensurePcStyles/ensureTsBadgeStyles/ensureMdliStyles + pc- 3 类/ts-badge/mdli-trunc-warn；tip left/top 与 show/hide display 属性级保留。**死代码分类修正（G3 复查清单扩容）**：ui-rows addWatchDirRow 3 处 + ui-preset buildPresetChipGroup/addClearRow 1 处经 ui-helpers 2026-08-23 清理记录实证无消费 re-export（生产零调用仅测试直测）——与 skeleton-utils 同型，不抽类。**批次12 tag-editor 1/1 + community render 1/1 + keymap 1/1 全清 = 活代码清零收官**（`95706c6e`，2026-09-04）：tag-editor box 挂 body light DOM——ensureTeStyles head 注入 + `.te-box` 补布局（dlg-box/dlg-pad 语义类保留，规则在全局 components.css）；community nameWrap→`.gh-name-wrap`（规则追加 content-diag.ts gh- 域）、keymap row→`.stg-km-row`（规则追加 content-stg.ts stg- 域）——两 css 域文件均**手编** contentCSS 组合层可追加（shadow adopted 生效）。**剩 8 处全为死代码/豁免/非批判**：ui-rows 3 + skeleton-utils 2 + ui-preset 1（死代码，移交 G3 复查）/render.ts 1（动态豁免，合规）/app-modules.test 1（测试环境重置 documentElement 内联，非 UI 渲染非 P1 批判范畴）。**P1 状态：✅ 活代码清零**（139 处中 125 处类化迁移 + 5 动态豁免 + 9 死代码/非批判转档），12 批次全部全量套件认证。 |
| P2 | fallback 硬编码中文散布（181 处） | 🟡 评估留档 | 查证：三语 key 集一致（locales-consistency 测试）+ LocaleKey 编译守卫，fallback 仅在动态 key 拼错时触发（设计双保险非缺陷）；触发即三语皆缺，改英文仅换受众，181 处搬迁 ROI 负——不动，语义注释已说明 |
| P3 | legacyTestId 永久背负 e2e 兼容映射 | ✅ 设限 | `node-types.ts:148` 定义处标 @deprecated + 淘汰期限 2027-06-30 + 迁移条件（e2e 改选节点 id/panelTestId 派生选择器）+ 过渡期禁新增；届期删字段并移除 core.ts:111,228 / roles.ts:89 映射输出 |
| P4 | mock bridge `as AppBindings` 类型断言 | 🟡 评估留档 | 查证：Proxy 壳将破坏 app.test.ts 2 处 toBe(mockApp) 身份断言 + stub 会引爆 E2E mock「缺失方法静默跳过」依赖；现值 P3 已挡空对象、缺失方法调用已抛 TypeError——低价值高扰动，暂缓 |

## 汇总

- 已闭环 11 项：严重 4/5（S1/S3/S4/S5）+ 一般 6/6（G1-G6）+ 风格 P1/P3（P1 活代码清零 12 批次 + 死代码复查删除）——剩 S2（严重）排期
- 严重项 S2：ADR-174 已立（判定规则单一源查证成立 + 对账硬锁策略 + 首轮四函数漂移审计），剩对账 fixtures 基建排期（收口定义见 ADR D5）
- 待处置：S2 fixtures 基建（B1c 已锁降级语义，ADR-174 D5 剩双端语料+镜像测试，go/ 并行静默后动）；P2 / P4（评估留档，不铺开）；知识卡 ui_components.md 清理 ui-preset 行（等并行改动落定）；P1 cssText 账面归零（死代码已删 + render 豁免 1 + 测试重置 1，均合规）；P3 仅剩「届期清退」动作挂在 2027-06-30
- 状态卡纠错（2026-09-03）：此前汇总误计「一般 6/6 / 共 10 项闭环」——G6（overlay light DOM）实为 ⏳ 排期且 git 无闭环提交，此处修正为 一般 5/6 + 共 9 项闭环
- 兄弟基线遗留：browser-adapter.contract-b2.test.ts:227,241 缺 desc（HEAD 即红，非本锐评引入），push 需兄弟收口或逃生阀
