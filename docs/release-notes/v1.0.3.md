## v1.0.3 — 导入修复 + 弹窗统一 + 代码重构

### 🐛 Bug 修复
- **整合包右键"从仓库导入模型"** — 原为导入到所有整合包，现修复为只导入到当前整合包
- **文件夹重命名/新建** — 空名称提交时显示红色提示"此项不能为空"
- **回收站"清空"** — 确认弹窗已正确使用 modalConfirm

### ✨ 体验优化
- **批量重命名** — 自动预填首文件的作者/作品到输入框；输入防抖 200ms 实时预览；预览表头增加「原名 → 新名」列标题
- **所有弹窗按钮** — 取消按钮标注 `(Esc)`，确认按钮标注 `(Enter)`，快捷键一目了然
- **空输入验证** — modalPrompt 空提交时显示红色内联提示，输入后自动消失

### 🧹 代码重构
- **`app-content/index.js` 拆分** — 2114 行巨无霸拆分为 5 个文件（1342 行主文件 + 4 个业务模块），职责单一，查找 Bug 更快
- **死代码清理** — 删除 `sync.js`、`dialogs/recycle.js/logs.js/settings.js/summary.js`、`ui/`、`versions/`、`lib/` 等 20+ 个未引用文件
- **遗留 `showConfirm` 统一** — 全部替换为 `modalConfirm`（之前全部静默失效）

### 📦 发布包
- 大小：10.6 MB
- 包含：`YSM-Model-Manager.exe` + `workshop_sites.json` + `workshop_creators.json`
