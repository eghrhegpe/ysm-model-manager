# v1.1.0 — 开发者对比（技术细节）

> 此文件记录实现细节，供开发者参考，不展示给用户。

## 🎨 CSS 工程化详情

将 ~50 处 `style.cssText` / `style="..."` 迁移为 CSS 类：

| 文件                      | 清理前        | 清理后            |
| ------------------------- | ------------- | ----------------- |
| `workshop-render.js`      | ~30 处 inline | 零 inline         |
| `workshop-site-view.js`   | ~30 处 inline | 3 处功能性 inline |
| `workshop-events.js`      | ~15 处 inline | 零 inline         |
| `workshop-data.js`        | ~6 处 inline  | 1 处动态 width    |
| `workshop-diagnostics.js` | ~4 处 inline  | 零 inline         |
| `app-preview/**`          | 全部 inline   | 全部 CSS 类       |
| `rename.js`               | 全部 inline   | 全部 CSS 类       |
| `batch-rename.js`         | 全部 inline   | 零 inline         |
| `app-sidebar/tpl.js`      | ~4 处 inline  | 零 inline         |
| `app-toast.js`            | ~2 处 inline  | 零 inline         |

主包体积：**219.82 KiB → 213.31 KiB**（-6.5 KiB）

新建 CSS 类前缀体系：`ysm-`（预览）、`ws-`（创意工坊）、`dlg-`（对话框）、`br-`（批量重命名）

## 🔧 构建

```powershell
.\build-release.ps1 v1.1.0
```
