# v1.2.0 — 开发者对比（技术细节）

> 此文件记录实现细节，供开发者参考，不展示给用户。

## 🎨 CSS 工程化详情

将 ~80 处 `style.cssText` / `style="..."` 迁移为 CSS 类，覆盖 15 个文件：

| 文件                      | 清理前        | 清理后            |
| ------------------------- | ------------- | ----------------- |
| `workshop-render.js`      | ~30 处 inline | 零 inline         |
| `workshop-site-view.js`   | ~30 处 inline | 3 处功能性 inline |
| `workshop-events.js`      | ~15 处 inline | 零 inline         |
| `workshop-data.js`        | ~6 处 inline  | 1 处动态 width    |
| `workshop-diagnostics.js` | ~4 处 inline  | 零 inline         |
| `app-preview/**`          | 全部 inline   | 全部 CSS 类       |
| `rename.js` / `modal.js`  | 全部 inline   | 全部 CSS 类       |
| `batch-rename.js`         | 全部 inline   | 零 inline         |
| `app-sidebar/tpl.js`      | ~4 处 inline  | 零 inline         |
| `app-toast.js`            | ~2 处 inline  | 零 inline         |
| `canvas-export.js`        | ~3 处 inline  | 零 inline         |
| `app-content/tpl.js`      | ~20 处 inline | 零 inline         |

主包体积：**219.82 → 212.17 KiB**（-7.6 KiB, -3.5%）

新建 CSS 类前缀体系：`ysm-`（预览）、`ws-`（创意工坊）、`dlg-`（对话框）、`br-`（批量重命名）、`repo-`（仓库）、`stg-`（设置）

> ⚠️ 修复：Shadow DOM 组件（`app-content`）的 CSS 类必须写在 `content-css.js`，全局 `components.css` 无法穿透。

## 🔧 构建

```
.\build-release.ps1 v1.2.0
```
