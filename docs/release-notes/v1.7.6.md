# v1.7.6 — 全量动画系统完成

**发布日期：** 2026-06-16

---

## 🎬 P0: 对话框入场/退场动画

所有模态对话框（重命名、标签编辑、批量重命名、高级筛选、确认框、输入框、选择框）现在支持：

- **入场**: overlay fade-in (150ms) + box scale(.95)→scale(1) 弹性放大 (200ms)
- **退场**: overlay fade-out (120ms) + box scale(1)→scale(.95) 缩小
- **防重复**: `_closing` 标志防止动画期间重复触发

**修改文件**:
| 文件 | 改动 |
|------|------|
| `components.css` | 新增 `dlgFadeIn/Out`, `dlgBoxIn/Out` 关键帧 + `.dlg-closing` 类 |
| `modal.js` | 新增 `closeDlg()` 工具函数，所有对话框统一使用 |
| `rename.js` | 导入 `closeDlg`，替换直接 `remove()` |
| `tag-editor.js` | 导入 `closeDlg`，替换直接 `remove()` |
| `adv-filter.js` | 导入 `closeDlg`，替换直接 `remove()` |
| `batch-rename.js` | 添加 `.dlg-closing` 类 + 延迟移除 |

## 🖱️ P0: 按钮 :active scale 反馈

所有按钮点击时缩小到 97%，提供触觉反馈。

**修改文件**:
| 文件 | 改动 |
|------|------|
| `components.css` | `.btn-base:active { transform: scale(0.97); }` |
| `shared-styles.js` | 同上（Shadow DOM 按钮） |

## 📄 P1: 页面切换淡入

导航切换时新页面从下方 6px 处淡入 (200ms)。

**修改文件**:
| 文件 | 改动 |
|------|------|
| `content-css.js` | `@keyframes pageIn` + `.page { animation: pageIn .2s ease; }` |

## 🔍 P1: 预览面板内容过渡

选中模型后预览内容从下方 4px 处淡入 (200ms)。

**修改文件**:
| 文件 | 改动 |
|------|------|
| `preview-css.js` | `@keyframes previewIn` + `.content > * { animation: previewIn .2s ease; }` |

## ⚙️ P2: 设置页高级面板展开/折叠

"高级设置"面板使用 max-height 过渡实现弹性展开/折叠。

**修改文件**:
| 文件 | 改动 |
|------|------|
| `content-css.js` | `@keyframes advPanelIn/Out` + `.adv-open/.adv-closing` 类 |
| `community-settings.js` | 使用 CSS 类替代 `display` 切换 |

## 🔄 P2: 同步管理器列表淡入

列表内容切换时添加 fade-in 过渡。

**修改文件**:
| 文件 | 改动 |
|------|------|
| `tpl.js` | `.sm-list { animation: fade-in .15s ease; }` |

## ♿ 无障碍规则更新

`.no-animations` 模式下禁用以上所有新动画。

**修改文件**:
| 文件 | 改动 |
|------|------|
| `variables.css` | 新增 `.dlg-overlay`, `.dlg-box`, `.page`, `.content > *`, `.btn-base`, `#set-advanced-panel`, `.sm-list`, `#dl-imported-list > div`, `.cr-card-hidden` 规则 |

---

## 📊 改动统计

| 指标     | 数值    |
| -------- | ------- |
| 修改文件 | 18 个   |
| 动画效果 | 15 种（全部完成） |
| 无障碍规则 | 16 条新增 |

---

## 🎯 P3 锦上添花（新增）

### 批量重命名预览脉冲
- `batch-rename.js` · `components.css`
- 预览行 stagger 入场（`brRowIn` + `animation-delay: ${i*15}ms`）

### 诊断面板切换淡入
- `community-diagnostics.js` · `content-css.js`
- 面板切换时重启 `diagPanelIn` 动画

### 回收站项目动画
- `recycle-bin.js` · `content-css.js`
- 列表 stagger 入场（`recyItemIn` + `animation-delay: ${i*25}ms`）
- 恢复/删除前 slide-out 退场（`.leaving` class）

### 资源管理器过渡
- `app-resource-manager/index.js` · `tpl.js` · `content-css.js`
- 列表 stagger 入场（`rmItemIn` + `animation-delay: ${i*20}ms`）
- 详情面板切换 fade-in（`rmContentIn`）

### 创作者卡片筛选淡出
- `community-site-view.js` · `content-css.js`
- `display:none` → `.cr-card-hidden` class，opacity/scale/max-height 过渡

### 导入队列无障碍
- `variables.css`
- 补充 `#dl-imported-list > div` 的 `.no-animations` 规则
