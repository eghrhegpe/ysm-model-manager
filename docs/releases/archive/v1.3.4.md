# v1.3.4 — 全局 CSS 加载修复（对话框/批量重命名/滚动条恢复）

> 发布日期: 2026-06-10

## 🎯 核心改动

### 🎨 修复全局 CSS 未加载导致 UI 样式丢失

`style.css` 从未被任何文件引用，导致 `components.css`、`layout.css`、`scrollbar.css` 等全局样式文件未被打包进构建产物，所有依赖全局 CSS 的 UI 元素（重命名弹窗、批量重命名、模态框、对话框、布局、滚动条等）样式丢失。

**修复**：在 `index.html` 中直接链接各 CSS 文件，不再依赖 `@import` 链。
