# v1.8.7 — Go 路径纹理映射补齐（WASM 对等能力）

**发布日期：** 2026-06-17

---

## 🗺️ Go 路径纹理映射补齐

使 Go 后端具备与前端 WASM 对等的纹理路径映射能力（3 文件 / +57 / -10）。

| 改动 | 文件 | 效果 |
|------|------|------|
| `archive.go` 路径映射增强 | `go/geometry/archive.go` | 纹理路径收集/映射补齐（33 行变更） |
| `extracted.go` 提取逻辑完善 | `go/ysm/extracted.go` | 提取结果路径对账（21 行变更） |
| `model3d.js` 同步适配 | `frontend/js/utils/model3d.js` | 前端 3D 渲染适配后端映射口径（13 行变更） |
