# v1.14.0 — 联邦渲染能力激活 + 3D 预览重构 + 同步去重核心

**发布日期：** 2026-08-28

---

## 🎉 贡献者

| 贡献者 | 改动 |
|--------|------|
| **@eghrhegpe** | 联邦阴影/灯光/Bloom 激活，mount3D MpSessionState 收敛，27 处硬编码 UI 接 t()，同步冲突解决 + 去重策略，网页版 Bedrock 预览补齐，CLI 白名单单源化 |

---

## 🚀 联邦渲染能力激活（ADR-073/084/107/108）

| 能力 | 说明 |
|------|------|
| 联邦阴影 | MMD/VRM/YSM soft shadow enabled — `shadowCap` 统一驱动 castShadow/receiveShadow |
| 联邦灯光调优 | volumetric + spotlight 激活，内容层包围盒 → 聚光灯/体积光锥瞄准 |
| Bloom/天空/地面 | 天空程序化纹理基线接入，地面 checker 纹理 repeat 升级（size 50→80），Bloom 后处理参数调优 |
| 联邦扩展点 | `federal.use(cap)` 一行接入，1 能力 = 1 文件，天空/灯光/bloom/godrays 均可独立启用 |

---

## 🛠 3D 预览重构

| 改动 | 说明 |
|------|------|
| MpSessionState 收敛 | mount3D 内 28 个裸 `let` 收敛为 `session: MpSessionState` + `infra: MpSharedInfra` 两对象，消除 infra 拆包层 |
| self 模式回归修复 | `infra!` 非空断言在 self 模式下 infra=null 直接抛异常；改条件访问 + `if (i)` 包整段 |
| 场景调优 | 联邦渲染能力视觉调优 — Bloom/天空/地面参数升级 + ADR-061 勘误 |
| 代际守卫统一 | GenGuard 类统一代际计数，bug#18 治理 |

---

## 🌐 国际化

| 改动 | 说明 |
|------|------|
| 27 处硬编码 UI 接 t() | maid-3d/detail-3d/detail/skeleton-fill-panel/perf-cli/app-tree/app-nav 全部接 t() |
| 新增 30 个 i18n key | preview.selectedModel/boneCount/cubeCount/textureCount/license/l0Roles/analyzingModel/bedrockModel/reference/sceneModelLabel/noOtherMorph/allMorphCount/stagePerfLabel/loadStage/stageEmpty/stageContents/noPackPng/blockItemModel3d + nav.resourceCategory/resourceType + perf.copyRaw/copy + skeleton.currentBinding/componentExclusive/slots + tree.fileList（三语同步） |
| i18n-ui-check 精度修复 | 删除 maskRegexLiterals 修复 wasm.ts 4 处误报，drift 31→27（全为真硬编码） |

---

## 🔀 同步/去重核心

| 改动 | 说明 |
|------|------|
| P1 冲突检测 | 哈希比较/配置接线/UI 入口/错误传播核心修复 |
| 同步冲突解决 | Wails 绑定 + 前端 UI（ResolveSyncConflict） |
| 去重策略配置 | 即时显示配置面板，消除重复 UI |
| SyncResourcesDirLevelScan | 复用 scanner 缓存消除重复 Walk，性能治理 |
| sync:toggle:status | 仅 YSM 树触发——非 YSM toggle 不再误触 YSM 同步链 |

---

## 🌐 网页版补齐

| 改动 | 说明 |
|------|------|
| Bedrock 预览/分析 | AnalyzeBedrockModel 支持 ysm.json manifest 多角色合并，对齐桌面声明序/纹理序 |
| 缩略图 binding | FindPreviewImage/ExtractPreviewTexture web 实现 |
| 功能缺口补齐 | 非根目录扫描/批量读取/资源包3D/目录预览/截图下载/工坊窗口回退 |
| parseYsmManifestMeta | catch 留 warn，不再静默吞 JSON 解析异常 |

---

## 🧹 工程治理

| 改动 | 说明 |
|------|------|
| CLI 白名单单源化 | 收敛 web-cli/cli-bridge 双硬编码到 cli-allowlist.ts 单一事实源 |
| Go diff 覆盖率门禁 | 集成 pre-push，60/80 分割，跨平台桥接文件豁免 |
| 契约测试路径修复 | 白名单字面量从 cli-bridge.ts 搬到 cli-allowlist.ts，契约测试同步更新 |
| Android capabilities | can() 改黑名单制，按真实可用性分类而非一刀切 false |
| 诊断页治理 | 隐藏桌面专属面板/CLI 性能入口，避免网页版可见但不可用 |

---

## 📊 统计

- **提交数：** 542（自 v1.13.2）
- **文件变更：** 674 files changed, +49846/-21133
- **净增能力：** 联邦阴影/灯光/Bloom 激活，同步冲突解决，去重策略配置，网页版 Bedrock 预览
- **净减：** 28 个裸 let 收敛为 2 对象，27 处硬编码中文接 t()

## 🔗 关联 ADR

| ADR | 状态 | 说明 |
|-----|------|------|
| ADR-061 | 🔄 部分采纳 | 3D 动画驱动（已接入 ysm-adapter，D1 落地） |
| ADR-073 | ✅ 已采纳 | 联邦渲染能力 — 天空/灯光/阴影/Bloom |
| ADR-084 | ✅ 已采纳 | 联邦能力注册表 |
| ADR-093 | ✅ 已采纳 | 多模型同框（Spec3D.models[] 支持） |
| ADR-100 | ✅ 已采纳 | YSM 动画播放器（clip 切换/淡入/Molang） |
| ADR-107 | 🔄 部分采纳 | 天空体积光 godrays（扩展点就绪） |
| ADR-108 | 🔄 部分采纳 | 相机包围盒构图（扩展点就绪） |
| ADR-119 | ✅ 已采纳 | dedup 并行哈希管道 |
| ADR-123 | ✅ 已采纳 | 跨平台桥接文件豁免 Go diff 覆盖率 |
