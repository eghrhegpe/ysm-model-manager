# v1.7.5 — 暗色模式自动切换 + 打开文件位置 + 调试代码清理

**发布日期：** 2026-06-16

---

## 🎨 系统暗色模式自动切换

- 验证 `matchMedia('change')` 监听器已正常工作，`theme === "system"` 时跟随 OS 自动切换 cyber ↔ warm
- 增强：切换时 toast 提示用户（如"已跟随系统切换至深色主题"）

## 📂 右键"打开文件位置"

- Go 端新增 `RevealInExplorer` binding，调用 `explorer /select,path` 在资源管理器中定位文件
- 仓库文件右键菜单添加"打开文件位置"菜单项

## 🧹 调试代码清理

清理 v1.7.4 遗留的 16 处调试日志：

| 文件 | 数量 | 内容 |
|------|------|------|
| `handler-dnd.js` | 2 处 | `[DnD]` console.log |
| `toolbar-events.js` | 1 处 | `[DBG:toolbar-bind]` console.log |
| `preview-utils.js` | 4 处 | `[YSM]` console.log（绕过 devLog 守卫） |
| `resource_bindings.go` | 9 处 | `[push]` fmt.Printf 流程追踪 |

---

## 📊 改动统计

| 指标     | 数值    |
| -------- | ------- |
| 修改文件 | 5 个    |
| 新增行数 | +24     |
| 删除行数 | -50     |
| 净增行数 | -26     |
