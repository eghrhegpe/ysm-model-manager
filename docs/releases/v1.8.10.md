# v1.8.10 — 发版脚本修复 + PR 前置准备

**发布日期：** 2026-06-17

---

## 🔧 构建流程修复

- `build-release.ps1` — 在 `wails build` 前新增 `go generate ./go/...` 步骤，确保 litematic 蓝图模块的 `block_ids_data.go` 自动生成
- `docs/README.md` — 新增 4 步构建发布流程文档

## 🐛 v1.8.5-v1.8.9 期间累积修复（本次随附发布）

| 修复 | 文件 |
|------|------|
| `GetRepoRoot("")` 返回错误 ✅ + 跨类型搜索兜底 | `resource_bindings.go` |
| `tryFetchModels` 第 2/3 请求并发失效（值拷贝 bug） | `data.js` |
| `gh-striped` 错写到 style 属性 | `data.js` |
| `avatar:refresh` 单卡片定点更新替代整页闪烁 | `index.js` |
| `app-sync-manager` 动画补齐 + 骨架屏可见性 | `tpl.js` `index.js` |
| `.no-animations` 补 `.sm-*` 5 个 class | `variables.css` |
| 标签存储 `go/tags/` 补 11 测试 | `tags_test.go` |
| 前端 Vitest 框架 + 30 测试（fmt/dom/data） | `package.json` `vite.config.js` `*.test.js` |

## 📚 文档

- `docs/TERMINOLOGY.md` — 术语对照表（UI 文案/代码字段/说明 三栏 + 禁止词清单）
- `AGENTS.md` — 补 OpenCode 模型索引（§八）、新增 4 个必读文档引用
- `docs/README.md` — 新增构建发布流程 + 术语表链接
- 清理 `_wails_test/` 遗留目录、v1.7.6 重复发版说明

## 📊 改动统计

| 指标 | 数值 |
|------|------|
| 修改文件 | 12 个 |
| 新增文件 | 6 个（测试 4 + docs 2） |
| 删除文件 | 3 个（`_wails_test/` + `v1.7.6.md` + `v1.7.7.md`） |
| 新增测试 | 41 个 |
