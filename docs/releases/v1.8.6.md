# v1.8.6 — 3D 渲染引擎重构 + 资源配置重构 + 调试清理

**发布日期：** 2026-06-17

---

## 🎨 3D 渲染引擎重构

渲染管线核心重构（39 文件 / +1240 / -1422），消除旧渲染路径的坐标变换反复修正。

| 改动 | 文件 | 效果 |
|------|------|------|
| `model3d.js` 渲染逻辑重构 | `frontend/js/utils/model3d.js` | 3D 模型渲染主链路重构（74 行变更） |
| `preview-css.js` 样式重构 | `frontend/js/components/app-preview/preview-css.js` | 预览区样式适配新渲染路径（49 行变更） |
| `threejs/spec.go` 坐标规格重写 | `go/threejs/spec.go` | 对齐 ysmview 算法口径，坐标变换规则收敛（289 行变更） |
| `preview-wasm.js` WASM 对接调整 | `frontend/js/components/app-preview/preview-wasm.js` | 预览渲染对接新管线 |
| `model3d.js` → `spec.go` 解耦 | `frontend/js/utils/model3d.js` `go/threejs/spec.go` | 前后端渲染口径统一 |

## 🧱 资源配置重构

| 改动 | 文件 | 效果 |
|------|------|------|
| 新增 `resource-registry.js` | `frontend/js/utils/resource-registry.js` | 前端资源注册统一入口（33 行新增） |
| watcher 同步逻辑调整 | `go/watcher/watcher.go` | 资源配置变更的监听同步（36 行变更） |
| `bedrock.go` 资源类型补充 | `go/types/bedrock.go` | Bedrock 资源类型对账 |

## 🧪 测试与清理

| 改动 | 文件 | 效果 |
|------|------|------|
| `mcmeta_test.go` 新增 | `go/packs/mcmeta_test.go` | 资源包元数据解析测试（167 行新增） |
| 调试输出清理 | 全仓 | 移除 1422 行调试/临时代码，净删除为正 |

## 📦 其他

- `frontend/js/features/recycle-bin.js`：回收站逻辑微调
- `frontend/js/dialogs/batch-rename.js`：批量重命名调整
- `frontend/wailsjs/go/models.ts`：绑定模型类型同步
- `ysm_config.json`：配置项调整
