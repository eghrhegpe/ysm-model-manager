# v1.9.0 — 文档宪法 + 路径大统一 + 主题增强 + 构建修复

**发布日期：** 2026-06-18

---

## 📚 文档体系重构（大项）

### AGENTS.md → 项目「文档宪法」

之前 AGENTS.md 只有 66 行，新版重写为 **215 行自包含入口文档**：

| 新增 | 说明 |
|------|------|
| §〇 索引即契约 | 5 条硬约束：禁止递归扫描 docs/、禁止读取 archive/、禁止 ls 探索、禁止全量读大文件、禁止随意创建 md |
| 完整文档地图 | 目录用途表 + 14 场景→文件速查表 |
| 致命陷阱 | 10 条最常踩的坑（Go 改后未重建、全局事件放错、按钮卡死等） |
| 治理红线 | window.\_\_\* 禁止、getApp() 统一、UI 安全规则 |
| 跨目录引用自检 | 断链 = 任务失败 |
| 启动约束模板 | 复制即用，锁住 Agent 行为 |

### archive/ 冷冻

30+ 个平铺文件按类别归入 4 个子目录：

```
archive/
├── postmortem/  9 篇    复盘记录
├── sessions/    4 篇    会话/开发笔记
├── design/      7 篇    计划/审计/重构
├── reference/   5 篇    分析/目录/变更
└── old/         8 篇    早期文档
```

### NAMING_GUIDELINES.md 新建

`docs/core/` 下新增命名规范，覆盖文档文件/目录/源码命名规则 + 8 个历史遗留文件对照表 + 新文件 checklist。

### 全局断链修复

7 个文件 20+ 处跨目录 Markdown 链接修复（`architecture/README.md`、`TASK_PLAN.md`、`SESSION_HANDOFF.md`、`PROJECT_STATUS.md`、`AI-MODEL-MATRIX.md`、`v1.8.9.md`、小说），3D 路径统一由 `docs/3D-RENDERING/` → `docs/3D/`。

### Reasonix 配置优化

`reasonix.toml` 省 token 调参：

| 参数 | 旧值 | 新值 | 效果 |
|------|------|------|------|
| `auto_plan` | off | **on** | 先 plan 再执行 |
| `compact_ratio` | 0.8 | **0.5** | 50% 压缩（原来 80% 才压） |
| `compact_force_ratio` | 0.9 | **0.65** | 65% 硬截断 |
| `allow` 中去掉 task | — | 子代理不再自动批准 |

---

## 🏗 加载路径大统一（来自 v1.8.11）

**之前**：4 条独立加载路径各维护自己的 cache→WASM→Go 三阶段。
**之后**：统一为 `preview-skeleton.js → loadModelData` 单一路径。

- 删除 320 行死代码（`index.js:_loadModel2D`，从未被调用）
- 统一 WASM/Go 加载入口
- 新增 `CacheModelAvatars` Go 绑定

---

## 🐛 Bug 修复

| Bug | 修复 |
|-----|------|
| `.json` 解压目录头像不显示 | WASM 解码时从 `avatar/` 目录加载 |
| 加密 `.ysm` 作者全丢 | 删除内层 `const` 遮蔽声明 |
| WASM 缓存误拦截重试 | 改为判断 `cached?.geometry?.bones?.length` |
| WASM 对 `.json` 也初始化 | 移到类型判断后 |
| WASM 重复解码 | `_wasmFailed` 缓存标记 |
| 头像文件路径双倍 | 判断是否已有 `avatar/` 前缀 |
| 无 `ysm.json` 不回退 Go | 主动回退 Go 处理 |
| 前端构建报错：import 路径断链 | 修复 events.js + core.js 共 4 处路径（workshop-icons 移入 community/ 子目录后未同步） |

---

## 🎨 UI / 主题

| 改动 | 说明 |
|------|------|
| 薄荷物语主题 | 新增 mint 主题（第 4 套） |
| 自动切换 | 支持 system/time 模式自动切换主题 |
| UI 重构 | 主题系统相关组件重构 |
| 术语表落地 | Toast `请先设置…` → `请先配置…` 24 处、tooltip 缩短、UI 专有名词替换 |

---

## 🧪 测试覆盖

- **Updater 测试**：`normalize` / `isNewer` / `splitVer` / `fetchExpectedHash` 单元测试
- **Go 包覆盖**：6→11 包，+33 tests
- **3D 模块重构**：`model3d.js` 拆分为 loader/screenshot + 删除死代码 `preview-3d.js`

---

## 📊 统计

| 指标 | 值 |
|------|-----|
| 修改文件 | 100+ |
| 增删行数 | +2634 / -13811 |
| 主要贡献 | 文档重构 70% + 路径统一 15% + 构建修复 10% + 测试 5% |
