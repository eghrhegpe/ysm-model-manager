# 路线图

## P0 — 下一版本必须

> ✅ 全部 P0 已完成

### 1. 整合包模型去重

- **现状：** ✅ **已实现** — 右键整合包 → "🔁 去重此整合包模型"
- **技术：** Go 端新增 `DeduplicateCustomDir(customDir string) (int, int, error)`，扫描 custom 目录按 SHA256 哈希分组，每组保留第一个，其余调用 `recycle.Move` 移入仓库回收站；前端 `showSummaryDialog` 显示去重统计

### 2. 仓库树右键菜单 → 启用/禁用

- **现状：** ✅ **已实现** — `frontend/js/ui/cm-tree.js` `showFileMenu()` 新增 `⛔ 禁用`/`✅ 启用` 菜单项（根据 `isBannedEntry()` 判断），调用 `window.go.main.App.ToggleModelEnable(path)` 后自动刷新树和整合包状态

### 3. 回收仓库模型后，整合包同步状态联动

- **现状：** ✅ **已实现（方案 B）** — 回收操作不会自动扫描整合包，但底部状态栏的 "▶️ 同步状态" 按钮（`btn-sync-toggle`）点击即可刷新所有整合包状态。
- **备选方案 A（未实施）：** 回收前扫描所有整合包 → 弹出提示 → 推荐同步

---

## P1 — 体验优化

> 所有 P1 体验优化已完成：
>
> - **仓库树文件项显示大小/日期** — 文件节点右侧 `.sz` 显示 `fmt(Size) + 日期`（今日显示时间，非今日显示月/日）
> - **安装/同步进度增强** — 顶部增加实时进度条 + 状态栏显示 `[整合包编号/总数] 名称 (文件/总文件 ✅成功 ❌失败)`
> - **首次启动引导** — 未设置路径时仓库区域显示欢迎面板，含「选择仓库目录」「选择游戏路径」按钮，双向绑定后自动加载

---

## P2 — 功能完善

> 所有 P2 功能已完成（批量操作统计清单、右键菜单精简、拖拽导入自动刷新、整合包右键菜单、统计面板+上传、安装按钮修复、配置持久化、链接处理）。

### 待补充功能

所有待补充功能已完成：

- [x] **回收站对话框增加"还原到原位置"时的路径预览** — 每行显示完整路径
- [x] **导入日志对话框增加"复制全部"按钮** — 一键复制所有日志到剪贴板
- [x] **统计面板点击数字可跳转/筛选** — 仓库模型数聚焦搜索框、待上传模型数触发上传按钮
- [x] **仓库树支持"新建文件夹"** — 文件夹右键菜单 → "📁 新建文件夹"，调用 Go 端 `CreateDir`
- [x] **仓库树拖拽移动文件** — 文件节点 `draggable`，拖拽到目标文件夹释放
- [x] **整合包卡片内模型列表可筛选/搜索** — 卡片内新增输入框，根据模型名实时过滤

---

## P3 — 代码质量与稳定性

> 所有 P3 项已完成：
>
> - **统一错误提示** — 所有 `alert()` → `showToast()`（带 ❌ 前缀）/ `showConfirm()`；`alert` 不再在代码库中出现
> - **清理未使用代码** — `ui/` 目录 6 个文件均正常使用，无遗留代码
> - **统一工具函数** — `isBannedEntry()`、`stripBan()` 从 `versions/data.js` 迁移到 `lib/parse.js`（与 `esc`、`fmt` 共存）；`data.js` 中已移除重复定义
> - **统一超时/错误包装** — `safeCall(fn, ...args)` 函数（15 秒超时），包裹 `sync.js` 中 `InstallModelTo`、`MoveToRecycle` 等关键 Wails 调用，超时自动标记 ⏱️
> - **参数化同步函数** — `doSyncAll(entries?, instances?, statuses?)`、`doSyncMissing(entries?, instances?, statuses?)`、`doDeduplicate(entries?)` 支持传入参数，不传时回退全局变量
> - **深层文件夹路径持久化** — `dirKey` 从单级名称改为 `prefixPath + "/" + child.name` 完整路径，展开/折叠状态正确恢复嵌套文件夹

---

## ⏳ 等待外部支持

- 基岩版模型支持 — 等待文档
- 创意工坊集成 — 等待文档

---

## P4 — 视觉与体验美化（待启动）

> 以下为 AI 建议的美化方向，当前阶段聚焦功能稳定，待 P0~P3 全部验收后再启动。

### 视觉品牌
- [ ] Logo 设计（YSM 像素风图标）
- [ ] 主题色定制（强调色替换纯白交互色）
- [ ] 字体优化（系统字体栈 + 思源黑体）

### 交互增强
- [ ] 网格/图标视图切换 + 模型缩略图
- [ ] 悬停快捷操作（右侧滑出图标按钮）
- [ ] 误操作撤销（最近 5 步历史）

### 进阶功能
- [ ] 冲突状态提示（仓库文件被外部修改）
- [ ] 标签系统 + 高级筛选

### 细节打磨
- [ ] 动态统计看板（数字跳动微动效）
- [ ] 启动动画 / 骨架屏
- [ ] 关于页（致谢墙 + GitHub 链接）

> 详细设计见 [`docs/ai-issues.md`](ai-issues.md#-ai-建议--美化与体验提升待实施)
