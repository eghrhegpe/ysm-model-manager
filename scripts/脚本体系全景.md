# 脚本体系全景

> 所有 Python 工具脚本的索引。被 `.agents/skills/` 中的 SKILL.md 调用。

---

## 按用途分档

### 生产级（`--json` 支持子代理消费）

| 脚本 | 调用方式 | 说明 |
|------|---------|------|
| `review.py` | `python3 scripts/review.py` / `--json` | 9 条治理红线 + 4 项额外检查 |
| `type-consistency.py` | `python3 scripts/type-consistency.py` / `--json` | resource_types.json vs JS 扩展名一致性 |
| `link-checker.py` | `python3 scripts/link-checker.py` / `--json` | 所有 md 内部链接断链检测 |
| `release-notes-gen.py` | `python3 scripts/release-notes-gen.py` | git diff + commit 归类 → 结构化 JSON |
| `bug-search.py` | `python3 scripts/bug-search.py <关键词>` / `--json` | 搜索 bug-chronicle.md |

### 实用级

| 脚本 | 调用方式 | 说明 |
|------|---------|------|
| `funcmap.py` | `python3 scripts/funcmap.py -o funcmap.md` | 提取 Go/JS 函数注释映射表 |
| `doctor.py` | `python3 scripts/doctor.py` | Go 编译 + 前端构建 + 文件 + 红线 + Git |
| `ultrawork.py` | `python3 scripts/ultrawork.py` | 一键三连：build → test → review → git |
| `comment-checker.py` | `python3 scripts/comment-checker.py` / `--json` | 注释质量（废话/JSDoc/TODO/调试日志） |
| `event-audit.py` | `python3 scripts/event-audit.py` / `--json` | EventsOn/bus.on 注册位置检查 |
| `binding-check.py` | `python3 scripts/binding-check.py` | Go 导出函数 vs wailsjs 绑定一致性 |

### 一次性/调试

| 脚本 | 说明 |
|------|------|
| `inspect_ysm.py` | YSM 文件格式诊断（合并 v1-v5 的统一版），`--json` |
| `compare_textures.py` | 纹理对比 |
| `check_*.py` | 站点/创作者数据检查 |
| `fix_*.py` | 数据修复（官方/vup 描述等） |
| `restore_nico.py` | Niconico 数据恢复 |
| `transform_creators.py` | 创作者数据转换 |
| `validate_data.py` | 数据校验 |

### 半成品

| 脚本 | 问题 |
|------|------|
| `safe-edit-service.py` | `do_GET` 备份逻辑是空 `pass`，不可用 |

---

## 覆盖场景

### YSM 文件解析（inspect_ysm.py）

| 场景 | 覆盖 |
|------|------|
| YSM V3 裸格式（`YSM` + null 分隔 preamble） | ✅ |
| YSGP V2 格式（BOM + `YSGP` magic + 文本头） | ✅ |
| UTF-8 BOM 前置（`\xef\xbb\xbf`） | ✅ |
| BOM 后紧跟 CRLF 换行（Windows 风格） | ✅ |
| 多段 `===` 分隔的文本头（Metadata/Tips/Export/Codec） | ✅ |
| ZIP 伪装 `.ysm`（`PK\x03\x04`） | ✅ |
| 嵌套 YSGP（binary 里又套一个 YSGP header） | ✅ |
| 二进制段全零填充 | ✅ |
| XXTEA 加密二进制 blob | ⚠️ 检测到但不解密 |
| 内嵌文件名搜索（7 种常见文件） | ✅ |
| 二进制熵检测（判断是否加密） | ✅ |

### 治理红线

| 红线 | 工具 |
|------|------|
| `window.__*` 全局变量 | `review.py R1` + `doctor.py` |
| 硬编码颜色 `#xxxxxx` / `rgb()` / `hsl()` | `review.py R5` + `doctor.py` |
| `innerHTML` 拼接 | `review.py R8` + `doctor.py` |
| `display:none/block` 做动画 | `review.py R4` |
| `window.go.main.App` 直接调用 | `review.py W2` + `doctor.py` |
| 反斜杠路径 | `review.py W1` |
| 空 JSDoc 模板 | `review.py W3` + `comment-checker.py` |
| TODO 无 ticket 编号 | `review.py W4` + `comment-checker.py` |
| magic string 资源类型 | `review.py R7` |
| 回调式 `.file()` API | `review.py R3` |

### 一致性校验

| 校验项 | 工具 |
|--------|------|
| `resource_types.json` ↔ `extensions.js` 双向比对 | `type-consistency.py` |
| Markdown 内部链接断链检测 | `link-checker.py` |
| `EventsOn`/`bus.on` 注册位置审计 | `event-audit.py` |
| AI 废话注释（「用于」「这是」「检查…是否」） | `comment-checker.py` |
| 注释掉的代码行 | `comment-checker.py` |
| `console.log` 调试残留 | `comment-checker.py` |
| Go 导出函数 vs wailsjs 绑定一致性 | `binding-check.py` |

---

## 已修复盲区

| 盲区 | 修复状态 |
|------|---------|
| CSS 变量覆盖率（`rgb()`/`hsl()`/3 位 hex） | ✅ `review.py R5` 已扩展 |
| Wails Binding 签名一致性 | ✅ `binding-check.py` 新建 |
| inspect_ysm 1-5 分散 | ✅ 合并为 `inspect_ysm.py` + `--json` |
